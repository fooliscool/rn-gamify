name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - develop

jobs:
  call-high-workflow:
    uses: fooliscool/rn-monorepo/.github/workflows/checks-high.yml@main
    secrets:
      PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
